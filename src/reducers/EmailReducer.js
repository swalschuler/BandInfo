// import from ../actions/types the types

/*

const INITIAL_STATE = {

};

export default(state = INITIAL_STATE, action) => {
  switch(action.type) {
    case TYPE:
      return { ...state, blah blah};
    default:
      return state;
  }
};
*/
